import * as ActionTypes from './actionTypes';

const actions = {
  // addToSelection: (e, _, dispatch, value) => {
  //   e.preventDefault();
  //   dispatch({ type: ActionTypes.ADD_TO_SELECTION, payload: value })
  // },
  // removeFromSelection: (e, _, dispatch, value) => {
  //   e.preventDefault();
  //   dispatch({ type: ActionTypes.REMOVE_FROM_SELECTION, payload: value })
  // },
}
export default actions;
